# 사랑흐름(LoveFlow) 프로젝트 전체 분석

> 관계·마음을 여행하는 감성 커머스 서비스 · 정적 HTML + Google Apps Script(GAS) 백엔드
> 도메인: **www.loveflow.ai.kr** (CNAME) · GitHub Pages 호스팅 (`hkcpcgg/loveflow`)
> 문서 작성일: 2026-07-19 · 최신 배포 계보: **v260719** (브랜드 슬로건 3층 교체)

---

## 1. 프로젝트 개요

**사랑흐름**은 소중한 사람과의 관계를 "여행"이라는 은유로 풀어내는 서비스다. 손님은 무료 진단으로 관계를 들여다보고, 유료 상품(여행지도·편지·자서전)을 예매해 자신만의 결과물을 받는다. 모든 손님은 **여권번호 `LF-XXXXX`** 하나로 식별되며, 이 번호가 전 화면을 관통하는 열쇠다.

| 항목 | 내용 |
|---|---|
| 서비스 성격 | 관계·마음 콘텐츠 커머스 (참고용 콘텐츠, 의료·심리치료 대체 아님) |
| 기술 스택 | Vanilla JS · 순수 정적 HTML (프레임워크 없음) · GAS 백엔드 |
| 백엔드 | Google Apps Script 4종 (발급/조회 · 편지 · 계좌접수 · 자서전 · 영문저장) |
| 데이터 저장 | 구글시트(원본 DB) + 브라우저 localStorage/sessionStorage(임시·이력) |
| 결제 | 서버 결제 미연동 — 계좌이체 + 네이버 스마트스토어 + 오픈마켓 우회 |
| 로그인 | 없음 (여권번호 = 계정, 기기 저장이 곧 신원) |
| 디자인 | 딥네이비 `#1E4A76` + 골드 `#D4AF37` + 크림, 412px 모바일 프레임 |
| 사업자 | 황규찬 · 파주시 매봉길 17-13 · 285-17-02228 · 통판 제2025-경기파주-1747호 |

---

## 2. 핵심 개념 — 여권번호 `LF-` ⚠️

전 화면을 관통하는 가장 중요한 규칙이며, 여러 파일 주석에 **절대 손대지 말 것**으로 반복 명시돼 있다.

- **발급은 `LF-` 하나로만** 한다. 식별자(PK)에 상품 의미를 넣지 않는다. (260713 이사회 만장일치)
- **수신·검증은 옛 `LFM-`/`LFL-`까지 전부** 받아준다. 정규식 `/LF[ML]?-/` 유지.
- **이 비대칭(발급은 좁게 · 수용은 넓게)이 의도된 정답**이다.
- 배경: 260710~13에 `LFM-`/`LFL-`로 바꿨다가 `'LFL-...'.indexOf('LF-') → -1`로 읽는 코드 8곳이 죽어 사흘간 도장·이메일·관계·별명·여권찾기가 전부 멈춘 사고가 있었다. 그 기간 실제로 나간 옛 접두어 여권 손님을 지키기 위해 수용 폭을 넓게 유지한다.
- 260707 설계서의 `LFM-`/`LFL-` 발급 규칙은 **폐기된 문서**이므로 되살리지 말 것.

**여권번호 조회 순서** (`lfPassport()`/`myPassport()` 공통): URL `?id=` → `sessionStorage.lf_passport` → `localStorage.lf_passport`.

---

## 3. 전체 사용자 동선

```
[표지]  cover.html (3D 관계우주) ──┐
                                   ▼
[홈]   index.html = 마음 한마디 (무료 · 바이럴 진입)
        │   └─ 카톡 발송 → m/index.html (수신자 열람) → 다시 홈으로 (바이럴 루프)
        │
        ├─[무료]  free.html → compass.html (진단) → free-result.html
        │         draw/index.html (마음 도화지) · en/index.html (영문)
        │
        ├─[구경]  showroom.html (갤러리)
        │
        └─[예매]  showcase.html (상품 진열) ──▶ pay.html (결제 선택)
                                                    │
   결제 → 발권 → 입장 → 여행 (4단계 진행바)         ▼
                    gate.html (여권 발급) ─▶ enter.html (입장 의식)
                    apply.html (계좌이체 접수 · SR-)      │
                    reenter.html (아는 번호로 재입장)      ▼
                    find/index.html (잊은 번호 찾기)   journey/index.html (여행지도 질문)
                                                          ▼
                                                   journey/result.html (여행일지)
                                                          ▼
                                                     l/index.html (꽃비 통로)
                                                          ▼
                                                   letter/index.html (편지 질문·완성)

[자서전]  memoir/index.html (소개) → ritual.html (의식·여권입력)
          → ask.html (질문·GAS저장) → book.html (결과물 견본/PDF원판)
```

---

## 4. 화면 카탈로그 (계열별)

### 4.1 진입 · 랜딩

| 파일 | 역할 | 특이사항 |
|---|---|---|
| `cover.html` | "관계 우주" 3D 몰입 표지 (Three.js r128) | CTA 행선지 **임시**(`/`, `/index_old.html`) |
| `index.html` ⭐ | 현행 홈 = **마음 한마디** (피아노 대상선택 → 작성 → 카톡 발송) | v21-17 최신. Kakao SDK, 편지 GAS 저장, 바이럴 루프 |
| `index_old.html` | 구 긴 설명형 홈 (보존본, 소개용) | 관계 자서전 "준비 중" 잔재 |
| `m/index.html` | 카톡 링크(`/m/?id=`) 수신 편지 열람 | index와 **동일 편지 GAS(DTLjdA)** 공유 |
| `start.html` | 유료 주문번호 확인 게이트 | ⚠️ 주문번호 검증 **미구현**(TODO), 구매처 버튼 `href="#"` |
| `universe.html` | 여권 조회 후 관계우주 대시보드 (완성도·계절·5축) | JSONP+`atob` GAS. 데모 `LF-7K2M9` |

### 4.2 무료 체험

| 파일 | 역할 | 특이사항 |
|---|---|---|
| `free.html` | 무료 나침반 소개(설득 랜딩) | API 없음. 학술 근거 표기(Bowen·Gottman 등) |
| `compass.html` | 무료 진단 본체 (관계·MBTI → 5문항 → 16유형 결과) | Chart.js/Kakao SDK. 이메일 저장시만 GAS `saveEvent` |
| `free-result.html` | `?id=LC-` 무료 결과 재조회 | JSONP+`atob` GAS. denied/notfound 분기 |
| `draw/index.html` | 삐뚤빼뚤 마음 도화지 (Canvas 그림+액자+QR 합성 공유) | ⚠️ **문서 2벌 중복**(734~1384행 죽은 사본). 라이브 미연결(대표 승인 대기) |

### 4.3 여행지도 · 편지 (유료 핵심 콘텐츠)

| 파일 | 역할 | GAS | 저장키 |
|---|---|---|---|
| `journey/index.html` | 여행지도 질문지 (29단계) | M1A `saveJourney`(POST 1회) + `lookupPassport`(재여행 차단) | `lf_journey_<여권>` |
| `journey/result.html` | 여행일지(결과 렌더) | M1A JSONP 읽기 + `markViewed`(POST). Kakao 공유 | 읽기 전용 |
| `l/index.html` | 편지 꽃비 통로(1.1초 후 `/letter/`) | 없음 | 없음(잠금 스크립트도 없음) |
| `letter/index.html` | 편지 질문(5각도)·완성(증명서) | 편지GAS `getLetter`(GET) + `saveLetter`(POST) | `lf_letter_<여권>` |

- 셋 다 head 최상단 **잠금 스크립트**(여권 없으면 `/gate.html`로 튕김). 단 `l/index.html`만 예외(무료 통로).
- `letter`의 `buildLetterAI()`는 **미연결(항상 null)** → 템플릿 폴백. AI 편지는 L5+ 예정.
- GAS 저장은 전부 `POST no-cors` **1회**(응답 못 읽어 재시도하면 중복 저장되므로 안 함).

### 4.4 여권 · 발권 · 접수

| 파일 | 역할 | GAS | 특이사항 |
|---|---|---|---|
| `gate.html` | 여권 **발급**(발권) | M1A `issuePassport`(JSONP) | ⚠️ 계좌이체 SR-는 입금 전 생성 → "돈 안 내고 발급" 구멍(규모시 GAS 대조 필요). `issuePassport.gs v5` 선배포 의존 |
| `enter.html` | 발급 직후 입장 의식(여권 표시) | 없음 | 데모 `LF-W8Z2Q` |
| `reenter.html` | 번호 **아는** 손님 재입장 | M1A `findPassport`(JSONP) | 편지 이어가기는 `/letter/` 직행 |
| `find/index.html` | 번호 **잊은** 손님 조회 | M1A `findPassport`(JSONP) | 뒤4 + 보조정보 2개 이상 필요 |
| `apply.html` | 계좌이체 접수 → SR- 접수번호 | 별도 접수GAS(...NzZw) `apply`/`applyStatus` | v16 검문소(입금확인 전 발급 거부). 채널값 `사랑흐름(직접결제)` 변경 금지 |

### 4.5 상거래 · 자서전

| 파일 | 역할 | 특이사항 |
|---|---|---|
| `pay.html` ⭐ | 결제 선택 (계좌이체/바로예매/오픈마켓) | 상세는 [pay.md](pay.md) 참조. 결제 미연동, 게이트로 위임 |
| `showcase.html` | 상품 진열장 (5종 → pay로) | memoir·life "오픈 예정" 비활성 |
| `showroom.html` | 여행의 기록(구경 갤러리, 가격 미표기) | 최대 파일(1007행). 자서전 견본 base64 임베드 |
| `memoir/index.html` | 자서전 소개 랜딩 | "진단/치유" 단어 회피 원칙 |
| `memoir/ritual.html` | 자서전 여는 의식(4단계·여권입력) | v9에서 시안 파일명 404 오연결 수정 |
| `memoir/ask.html` | 자서전 질문(7장 16문항)·GAS 저장 | ⚠️ 확장차단 대비 GAS `saveAll` **미완**. Web Speech 낭독 |
| `memoir/book.html` | 자서전 결과물 견본(A4 PDF 원판) | JS 없음. "지어낸 본문 금지" 원칙 vs 견본 본문 재삽입 |
| `memoir/gatecheck.html` | 자서전 GAS 게이트 진단(개발용) | `LF-TEST9` 검증 유틸 |

### 4.6 정보 · 법적 · 기타

| 파일 | 역할 | 특이사항 |
|---|---|---|
| `about.html` | 서비스 소개(철학·5축·세계관·사업자) | `lf/brand.js` 시범 로드(`data-lf="creed"`) |
| `trust.html` | 법적 고지 통합(약관·개인정보·환불·FAQ) | 조항 텍스트 "절대 불변" 관리 |
| `history.html` | 나의 여행기록(localStorage `lf_history`) | ⚠️ **구버전 잔재**(구 4항목 메뉴, lfMineGrp, 폐기 링크 잔존) |
| `myinfo.html` | 실제로는 여행기록 목록(history v5) | ⚠️ 파일명↔내용 불일치. 구형 GAS·네이버 직링크 잔존 |
| `certificate.html` | 실제로는 유료 여행지도 진단 앱 | ⚠️ 구형 GAS(DTLjdA)·구형 상단바 미정리 |
| `share.html` | SNS 공유 카드 시안 | 목적지 전부 `/compass.html` |
| `admin.html` | 관리자 입금확인·여권발급 도구 | 게시판 GAS(JSONP). **열쇠는 GAS 내부**. noindex |
| `email-notice.html` | 이메일 수집 안내 인서트 | `/privacy.html` 부재로 링크 임시 제거 |
| `en/index.html` | 영문 무료 진단 | 별도 GAS(Base64). 영문 유료 CTA 전체 `display:none` |
| `pptest.html` | 여권 이미지 저장 테스트(개발용) | 격리 테스트본 |
| `robots.txt` | 결제·결과·개인 페이지 8종 크롤 차단 | |
| `sitemap.xml` | 공개 6종 등록(`/`·compass·free·about·en·trust) | |

---

## 5. 백엔드 (Google Apps Script)

로그인/서버 없이 GAS 4~5종으로 운영한다. 클라이언트는 대부분 **JSONP**(카카오 크롤러 차단 위해 GAS URL을 `atob` Base64 디코딩) 또는 `POST no-cors`로 호출한다.

| GAS | 용도 | 사용 화면 | 주요 action |
|---|---|---|---|
| **M1A** (`...9j4hxBZM1A`) | 여권 발급·조회 | gate, find, reenter, certificate, journey, universe | `issuePassport`, `findPassport`, `lookupPassport`, `saveJourney`, `markViewed` |
| **편지** (`...6DTLjdA`) | 마음 한마디·편지 | index, m, letter, (구)history | `get`(한마디), `getLetter`, `saveLetter` |
| **접수** (`...NzZw`) | 계좌이체 접수 | apply | `apply`, `applyStatus` |
| **자서전** (`...JSELNi3T6`) | 자서전 저장 | memoir/ask, memoir/gatecheck | `getMemoir`, `saveChapter` (⚠️`saveAll` 미완) |
| **게시판/관리자** (`...aUOkw`) | 관리자 목록·발급 | admin | `adminList`, `adminPaid` |
| **영문** (`...zdxMyJ`) | 영문 이벤트 저장 | en | `saveEvent` |

---

## 6. 공용 부품 (전 화면 재사용)

여러 화면에 **동일 스크립트를 인라인 복붙**하는 방식으로 통일한다(외부 JS 주입 폐기 — "파일만 열어도 보여야 한다"는 대표 지시).

- **☰ 표준 드로어 메뉴 (11항목)**: 홈 · 마음 한마디 · 삐뚤빼뚤 마음 도화지 · 여행지도 보기 · 편지 · 자서전 · 여권으로 입장 · 여권 찾기 · 여행의 기록 · 마음 여행 예매하기 · 다시 시작. `lfGo(k)` + `LF_LINK` 라우팅, 여권번호 자동 전파(`LF_NEEDS_ID`).
- **방향버튼 (`lfPaintWay`)**: `← 뒤로 · ⌂ 홈 · → 앞으로`. "앞으로"는 화면마다 다른 **매출 동선**(`LF_NEXT`). `© 2026` 저작권 덩어리를 찾아 그 앞에 삽입.
- **지난 여행 이어가기 (`lfPaintResume`)**: 입구마다 배치하는 "주인 버튼". 여권번호 입력 시트를 그 자리에서 열어(결제 이탈 방지) localStorage 초안 조회. ⚠️ **기기 간 이어가기 불가**(localStorage 한정) — 정직하게 명시.
- **여행자 발자취 티커 + 공유 버튼**: 마스킹 여권카드 마퀴 + `navigator.share`→클립보드 공유(공유 URL은 항상 홈).
- **`lf/brand.js`**: 브랜드 슬로건 3층(간판/철학/제품)을 한 파일에서 중앙관리. `data-lf` 이름표가 붙은 요소를 덮어씀. 안전장치: 파일이 안 열려도 HTML 원문이 그대로 보임.

**localStorage/sessionStorage 표준 키**: `lf_passport`, `lf_unlock`, `lf_here`/`lf_prev`(방향), `lf_journey_<pp>`/`lf_letter_<pp>`(초안), `lf_apply`(접수), `lf_history`(이력), `lf_admin_key`.

---

## 7. 디자인 시스템

- **색**: 딥네이비 `#1E4A76`(메인·상단바), 골드 `#D4AF37`(강조·가격 테두리), 크림 `#FFFBF5`(배경), 코랄 `#FF7A59`, 네이버 `#03C75A`, 카카오 `#FEE500`.
- **레이아웃**: 최대 폭 412px 폰 모형, sticky 딥네이비 상단바(로고+상호·‹뒤로·화면이름 정중앙·☰).
- **버튼 패턴**: 채움 1개 + 테두리 N개 (추천 경로만 진한 채움으로 유일하게 강조).
- **폰트**: Noto Sans/Serif KR, Nanum(명조·펜스크립트), 상황별 손글씨.

---

## 8. 미완 · 정리 대기 항목 (TODO)

### 🔴 기능 미완
- `start.html` — 주문번호 GAS 검증 미구현(TODO 주석), 구매처 버튼 `href="#"`.
- `pay.html` — 토스페이먼츠(바로예매) 연동 대기, `letter` 상품 스토어 URL 빈 값.
- `letter/index.html` — `buildLetterAI()` AI 편지 미연결(템플릿 폴백).
- `memoir/ask.html` — 확장 차단 기기 전량 저장 보장용 GAS `saveAll` 미완.
- `showcase.html` — memoir·life 상품 "오픈 예정"(미출시).
- `draw/index.html` — 라이브 어디에도 미연결(대표 승인 대기).

### 🟠 보안/정합
- `gate.html` — 계좌이체 SR- 입금 전 발급 구멍(규모 확대 시 GAS 입금대조 필요).
- 기기 간(폰↔PC) 이어가기 미지원 — 서버(GAS) 저장 필요, 위험구간이라 별건.

### 🟡 구버전 미정리 (리뉴얼 미반영)
- `certificate.html`, `myinfo.html`, `history.html` — 260713~19 통일 리뉴얼(표준 메뉴·`/l/` 링크·표준 상단바)이 미반영. 구형 GAS 편지주소(DTLjdA)·네이버 직링크·lfMineGrp 잔존.
- `draw/index.html` — 문서 2벌 중복(734~1384행 죽은 사본) 정리 필요.
- `cover.html` — CTA 행선지 임시값.
- `/privacy.html` 부재 — email-notice 링크 임시 제거(실제 개인정보는 trust.html#privacy).

---

## 9. 버전 관리 방식 (특이)

이 프로젝트는 **HTML 상단 주석에 상세 변경 이력을 축적**하는 독특한 방식으로 운영된다. 각 파일 헤더에 대표·이사회 발언과 결정 근거, "절대 손대지 말 것"/"되살리지 말 것" 경고가 서사적으로 기록돼 있다. 최신 계보는 **v260719**(브랜드 슬로건 3층 교체: 간판「가장 소중한 사람을 더 알아가는 시간」/ 철학「우리의 관계는 이미 시작되었습니다」/ 제품「서로의 언어를 읽어갑니다」).

---

*이 문서는 프로젝트 40여 개 HTML + `lf/brand.js` 정적 분석 종합 결과다. 실제 점수 계산·저장·발급 로직 상당수는 외부 GAS(구글시트 백엔드)에 위임되어 있어, GAS 소스는 이 저장소에 포함되지 않는다.*
